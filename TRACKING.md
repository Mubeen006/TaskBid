# TaskBid — Build Tracking

> Updated by the Coding Agent after every completed feature. Read this before starting any task.
> Status values: `NOT STARTED` → `IN PROGRESS` → `READY FOR TEST` → `TESTED — READY FOR COMMIT` → `COMMITTED`

| # | Feature | MD File | Status | One-line Summary | Notes / Deviations |
|---|---|---|---|---|---|
| 01 | Project Setup (repo, Docker Compose w/ replica set, env config) | `docs/features/01-project-setup.md` | TESTED — READY FOR COMMIT | Replica set confirmed ok:1 (healthcheck auto-init). Backend Express skeleton + /health route. Deps installed, .env from .env.example. | Replica-set initiation is automatic via Docker Compose healthcheck — no manual step needed. |
| 02 | Database Schema (Mongoose models: users, tasks, bids, auditlogs) | `docs/features/02-database-schema.md` | TESTED — READY FOR COMMIT | All 4 models + guard hooks implemented. Added pre('findOneAndUpdate') guard to both Task and Bid models (Feature 02 updated spec). 4 migrations applied. Indexes verified via mongosh. | Only 1 of 5 constraints (bid unique index) is true DB-engine-level. Guard hooks now cover both pre('save') and pre('findOneAndUpdate'). **Known minor gap, being closed by Feature 08's addendum** — see deviations log below. |
| 03 | Seed Script | `docs/features/03-seed-script.md` | TESTED — READY FOR COMMIT | Seed rewritten with correct pattern: tasks start 'open', Bid.create() while open, status advanced via setStatus()+save(). All 7 statuses covered incl. review. 5 users / 10 tasks / 15 bids. Idempotent (ran twice, same result). | Race-condition test task: 'Migrate legacy auth module' (bidding_closed), lowest bidder Bilal (2h) — a near-capacity user, confirmed suitable for Feature 09's Part-A test. Near-capacity users: Bilal (13/15h), Usman (8/10h). |
| 04 | Validation Schemas (Joi, shared middleware) | `docs/features/04-validation.md` | TESTED — READY FOR COMMIT | validate middleware (abortEarly:false, coerces req[target]), currentUser middleware (missing/malformed/nonexistent → 400), objectIdSchema util, tasks.schema.js, bids.schema.js, users.schema.js. mongoSanitize wired in app.js. 11/11 self-tests pass. | Amended post-signoff: `stripUnknown: true` added to the shared `validate()` factory — discovered during Feature 06 testing. Applies globally (body/params/query, no target-specific branch). Regression-checked against Feature 05's `GET /api/users/:id/workload` endpoint: 4/4 tests pass, no behavior change. |
| 05 | Users Module (workload endpoint) | `docs/features/05-users-module.md` | TESTED — READY FOR COMMIT | capacity.js (getRemainingCapacity, hasCapacityFor — pure, no DB), users.repository.js, users.service.js, users.controller.js. GET /api/users/:id/workload mounted. 8/8 capacity unit tests pass. Live endpoint: valid→200, nonexistent→404, malformed→400. | No deviations. ESLint clean (0 errors). Re-verified 4/4 after Feature 06's stripUnknown fix — no regression. **Post-signoff addition (added during Feature 12, without a prior BLOCKED report as instructed — flagged here for the record):** GET /api/users added to users module (findAll in repository, listUsers in service, GET / in controller) returning `{ _id, name }` only. Required by the frontend user-switcher because user IDs are dynamically generated per seed and cannot be hardcoded. Read-only endpoint, no auth required. Also added to Swagger spec. |
| 06 | Tasks Module (create, status transition, list) | `docs/features/06-tasks-module.md` | TESTED — READY FOR COMMIT | tasks.repository/service/controller. POST /api/tasks, PATCH /api/tasks/:id/status, GET /api/tasks (+ optional ?status= filter). Mounted in app.js. Service-level isLegalForwardTransition check + setStatus()+save(). Status-transition now transaction-wrapped with its audit write (Feature 08 addendum). 8/8 original self-tests + retrofit rollback test all pass. | Option (a) for GET — bid-count/lowest-bid closed by Feature 07. No GET /api/tasks/:id. stripUnknown:true (attributed to Feature 04's row). Retrofit confirmed: forced mid-transaction error rolled task.status back to previous value with no orphan audit entry. |
| 07 | Bids Module (place bid, list bids) | `docs/features/07-bids-module.md` | TESTED — READY FOR COMMIT | bids.repository/service/controller. POST+GET /api/tasks/:id/bids mounted in app.js. placeBid uses hasCapacityFor (soft 422), translateBidCreateError maps guard hooks + MongoDB 11000. GET /api/tasks extended with bidCount/lowestBidHours. Bid-creation now transaction-wrapped with its audit write (Feature 08 addendum). 9/9 original self-tests + retrofit rollback test all pass. | Feature 06 follow-up closed. No status/assignment mutation. `session: null` gap in bids.model.js's pre('findOneAndUpdate') guard CLOSED — now reads `this.getOptions().session` in guardSelfBidAndBiddingOpenQuery. Retrofit confirmed: forced Bid-creation abort leaves no orphan bid or audit entry. |
| 08 | Audit Log Module | `docs/features/08-audit-module.md` | TESTED — READY FOR COMMIT | audit.repository.js + audit.service.js built, session-aware. Retrofitted into task creation (non-transactional, correct per Phase 7), task status-transition (transactional), bid creation (transactional). All three produce exactly one audit entry, verified by direct DB query. Illegal transitions/duplicate bids produce zero audit entries. ESLint 0 errors/warnings. | Addendum (transactional retrofit per Architecture Phase 7) fully implemented and tested — see Features 06/07's notes for the specific rollback proofs. Task creation intentionally non-transactional, per Phase 7 scope. |
| 09 | Assignment Engine (transaction + optimistic concurrency) | `docs/features/09-assignment-engine.md` | TESTED — READY FOR COMMIT | assignment.repository/service/controller built. Full algorithm verified via code review: version-conflict (return + retry-whole-attempt) vs capacity-insufficient (continue to next bidder) are structurally distinct, correctly implemented. readConcern:snapshot + writeConcern:majority. Bounded 3-attempt retry loop with TransientTransactionError/UnknownTransactionCommitResult detection. Happy/fallback/no-eligible-bidder/wrong-status paths all pass. **Part-A concurrency test: 10/10 runs pass, retry path confirmed hit in every run (20 log lines = 2 calls × 1 retry each per run), never overflow, capacityVersion incremented exactly once per successful assignment.** | Reuses hasCapacityFor/getRemainingCapacity (Feature 05) and recordChange with session (Feature 08) by name — confirmed in code. `Bid.updateMany()` used for marking other bids not_selected — deliberate, scoped exception (no guard-hook protection applies to this status-only flip on already-validated bids). Full assignment.service.js reviewed line-by-line by the architect. |
| 10 | Dashboard Module (aggregation pipelines) | `docs/features/10-dashboard-module.md` | TESTED — READY FOR COMMIT | GET /api/dashboard/stats. Four separate aggregation pipelines run via Promise.all. All 4 metrics verified against seeded data. ESLint clean. | Parallel four-pipelines design is deliberate per Architecture Blueprint Phase 16 (readability + independent index usage over marginal round-trip savings). Baseline values against fresh seed: tasksByStatus={assigned:1, bidding_closed:1, done:3, draft:1, in_progress:1, open:2, review:1}; avgBidByComplexity={c1:3h, c2:5.5h, c3:4h, c4:6.5h, c5:3.33h}; topUsersByCompleted={Sara Malik:2, Usman Tariq:1}; zeroBidPastDeadline=["Archive old customer records" (deadline 2026-07-23)]. These are the reference values for Feature 16's Dashboard UI. |
| 11 | Swagger/OpenAPI Setup + Backend Error Handling Pass | `docs/features/11-swagger-error-handling.md` | TESTED — READY FOR COMMIT | swagger-ui-express@5.0.1 + swagger-jsdoc@6.2.8 installed. Hand-written OpenAPI 3.0.3 spec at backend/src/docs/swagger.js. All 10 endpoints documented. /api-docs live and returns HTML. 12-point error-handling consistency test passed. Guard-hook discriminator fix applied post-signoff. **Post-Feature-19 fix:** `servers` array updated to list both Railway production URL and localhost, ordered by NODE_ENV. | Spec approach: hand-written. Error-handling pass: all clean. **Mutating-endpoint regression pass (fully closed):** OPTIONS preflight confirmed 204 on both server entries with correct ACAO/ACAM headers. POST /api/tasks/:id/bids (Ayesha, 2h, "Build reporting dashboard") against localhost → **201 Created** with populated `user: { _id, name }` in response. Railway POST → **409 CONFLICT** (Ayesha already bid from concurrency tests — not a CORS failure: response has correct ACAO header + JSON error envelope, meaning request reached the application layer). No CORS/preflight issues on either server entry. ESLint 0 errors. |
| 12 | Frontend Setup (Vite, routing, layout, user-switcher) | `docs/features/12-frontend-setup.md` | TESTED — READY FOR COMMIT | React shell wired: BrowserRouter + QueryClientProvider + CurrentUserProvider + AppLayout + 3 placeholder pages. api/client.js (native fetch, ApiError). CurrentUserContext with sessionStorage. All routes verified. API smoke tests pass. | **fetch chosen** (not axios) — standing convention for Features 13/14/16. Users sourced from GET /api/users (see Feature 05 notes for the endpoint's addition record). sessionStorage persistence confirmed: selection survives page refresh. ESLint clean on all modified backend files. |
| 13 | Task Board Page | `docs/features/13-task-board.md` | TESTED — READY FOR COMMIT | Kanban board with 7 status columns, client-side grouping, TaskCard/TaskColumn/StatusBadge components, useTasks React Query hook, tasks.api.js. Skeleton loading state, error alert on fetch failure, empty-column state. All 10 seeded tasks verified in correct columns. | date-fns@4.1.0 installed. Baseline confirmed: draft[1] open[2] bidding_closed[1] assigned[1] in_progress[1] review[1] done[3]. "Build reporting dashboard" bidCount=3 lowestBidHours=4 matches Feature 07 baseline. Zero-bid card ("Archive old customer records") shows "No bids yet". StatusBadge is reusable — will be consumed again in Feature 14. CreateTaskModal deferred (not required by acceptance criteria). oxlint 0 errors. |
| 14 | Task Detail Page + Bid Form | `docs/features/14-task-detail.md` | TESTED — READY FOR COMMIT | Full task detail page: task info, bid list, bid form with client-side capacity pre-check, stale-capacity 422 handling, all bid-disabled states, assign button, advance status button. react-hook-form@7.56.4. All 11 acceptance criteria tests passed. | **Single-task fetch: option (a)** — reuses GET /api/tasks React Query cache, `select` finds task by id. **Post-signoff fix (bid names):** bids.repository.findByTaskId now chains `.populate('user','name')`. bid.user is `{ _id, name }` in HTTP response and in socket event (bids.service.js populates before emitting BID_CREATED). BidRow reads `bid.user?.name`. BidList/BidForm use `getUserId()` helper handling both populated-object and raw-ObjectId shapes. **Regression pass (all 5 error paths re-confirmed):** self-bid 403 FORBIDDEN ✓, duplicate 409 CONFLICT ✓, bidding-closed 409 CONFLICT ✓, over-capacity 422 UNPROCESSABLE ✓, stale-capacity 422 UNPROCESSABLE ✓. Immediate-render path confirmed: Hina bids → GET returns `{ _id, name:"Hina Riaz" }` → getUserId() extracts correct _id. Realtime path confirmed: Sara bids from Tab A → sB receives bid:created with `user: { _id, name:"Sara Malik" }` not ObjectId — Tab B sees name regardless of acting user. **Deployment note: Railway must be redeployed after commit for the fix to be live on production.** |
| 15 | Realtime Layer (Socket.IO, both sides) | `docs/features/15-realtime.md` | TESTED — READY FOR COMMIT | Socket.IO server on raw http.createServer. Room-per-task join/leave. bid:created and task:assigned emitted after commit. Frontend singleton + useRealtimeBids hook with cache-merge + reconnect reconcile. All automated tests + actual backend restart reconnect test passed. | **getIO() pattern** used for io access from services — io set at boot in server.js via initSocket(httpServer), services call getIO() at emit time, no circular requires. **CORS dev-mode fix:** socket.js uses `origin: "*"` when NODE_ENV=development (production keeps CORS_ORIGIN). **Reconnect test performed with actual backend process kill/restart (not client-side simulation):** process killed via netstat+taskkill, new child process spawned, client auto-reconnected, re-joined room via onReconnect handler, received bid:created event (hoursOffered=6) — confirmed end-to-end without page refresh. socket.io-client@4.7.5 added as devDependency on backend for test script only (test script deleted after passing). oxlint + ESLint 0 errors on all new files. |
| 16 | Dashboard Page + Chart | `docs/features/16-dashboard-ui.md` | TESTED — READY FOR COMMIT | DashboardPage with all 4 metrics. Bar chart (avgBidByComplexity) via Recharts. StatCard, ChartPanel components. useDashboardStats hook. All 4 metrics match Feature 10 baseline exactly. oxlint 0 errors. | recharts@2.15.3 installed. Chart: complexity (C1–C5) on x-axis, averageHours on y-axis — all 5 bars verified against baseline: C1=3h C2=5.5h C3=4h C4=6.5h C5=3.33h. ChartPanel is generic (xKey/yKey/barColors props) — not hardcoded to complexity shape, usable for future charts. tasksByStatus rendered with mini progress bars. topUsersByCompleted: Sara Malik:2 / Usman Tariq:1. zeroBidPastDeadline: "Archive old customer records". All 4 empty states, skeleton loader, and error alert implemented. |
| 17 | Full Integration Pass + Concurrency Test | `docs/features/17-integration-testing.md` | TESTED — READY FOR COMMIT | 48/48 automated integration checks passed across 9 sections. No application code changes required — all issues found were test-script corrections only. | **Full checklist outcomes:** §1 Health+seed: PASS (10 tasks, 5 users). §2 Task lifecycle draft→done: PASS (all 5 intermediate statuses, both illegal-jump paths). §3 Bidding scenarios: PASS — self-bid 403, duplicate 409, bidding-closed 409, over-capacity 422, stale-capacity 422. **Note:** self-bid guard fires at 403 only when hoursOffered is within bidder's remaining capacity — if bid exceeds capacity, the 422 capacity guard fires first (by design, both guards are correct; test must choose hoursOffered carefully). §4 Part-A concurrency + realtime COMBINED, 5 runs: PASS — HTTP[200,200] every run, Bilal 13→15h, capVer+1, realtime task:assigned received on both tabs every run. §5 Assignment edge paths: PASS — wrong-status 409, no-eligible-bidder 422, task stays bidding_closed with null assignedUser. §6 Dashboard: PASS — tasksByStatus total matches DB count, all 4 keys present, metrics accurate. §7 Realtime reconnect: PASS — disconnect/reconnect/rejoin cycle confirmed. §8 MongoDB-specific: PASS — 11000→409 translation clean, no raw error leaked; transient-retry not forceable deterministically but confirmed in earlier passes via server log lines. §9 Constraint-honesty: PASS — bid unique index is only true engine-level constraint (11000 confirmed); self-bid/bidding-open/forward-only-status are Mongoose hook-level only (raw insert bypasses self-bid — expected, documented). isGuardViolation=true regression clean. capacityVersion resets to 0 on seed, increments atomically. Audit entries written on assignment. **Findings flagged (not bugs — documented facts for Feature 18 README):** (a) Capacity guard fires before self-bid guard when bid exceeds remaining capacity — both guards are correct, ordering is a consequence of service-layer check-order. (b) Bid.create() on bidding_closed tasks correctly triggers pre('save') guard — raw test fixtures must use Bid.collection.insertOne(). (c) Transient-transaction retry path cannot be deterministically forced at integration level; confirmed from server logs in Feature 09 and earlier passes. |
| 18 | README + DECISIONS.md | `docs/features/18-readme-decisions.md` | TESTED — READY FOR COMMIT | README.md and DECISIONS.md written at repo root. All 18 README sections present. DECISIONS.md covers Parts A/B/C(flagged)/D + architecture rationale + stack-deviation note. All facts sourced from named TRACKING.md rows. | Part C explicitly flagged as an interpretation (dashboard aggregation-pipeline design used as best-fit content; original assignment brief's Part C topic not defined in blueprint docs — one-line note added for project owner to confirm or correct). Live URL and Deployment sections clearly marked as pending Feature 19 — will need follow-up edit post-deployment. API endpoint count in README is 10 (not 9) — GET /api/users was added to the Swagger spec during Feature 12. No application code changed. |
| 19 | Deployment (Atlas + Render + Vercel) | `docs/features/19-deployment.md` | TESTED — READY FOR COMMIT | Atlas M0 provisioned (replica set atlas-2eyh7k-shard-0, MongoDB 8.0.28). 4 migrations + seed applied. Backend on Railway, frontend on Vercel. All smoke tests pass. Live concurrency 3/3 PASS. | **Backend deployed on Railway** (project owner chose Railway over planned Render — render.yaml kept in repo for reference). Frontend on Vercel. Network access: 0.0.0.0/0 — deliberate simplification, documented in README. CORS_ORIGIN on Railway set to https://task-bid-mocha.vercel.app (specific origin, not wildcard). Live concurrency test: 3/3 runs Bilal 13→15h, capVer+1, no overflow — identical to local results. Cold-start: Railway does not spin down on free tier; ~1.4s cold, ~600ms warm. ≥2-week stability flagged as an ongoing condition, not a checkbox. README.md Live URL and Deployment sections updated (only those two sections, no other file touched). |
| 20 | Bonus Items (if time remains) | `docs/features/20-bonus.md` | NOT STARTED | — | — |

---

## Current Focus
**Feature #20 NOT STARTED — Bonus Items (if time remains).** Features 01–19 are TESTED — READY FOR COMMIT. Full stack is live at https://task-bid-mocha.vercel.app.

## Blocked Items
None.

## Deviations From Blueprint (running log)
- **Decision (found during Feature 02/03 implementation review):** Feature 02's guard hooks (self-bid, bidding-open, forward-only-status) must be duplicated as `pre('findOneAndUpdate')` query middleware, not just `pre('save')` document middleware — otherwise any service/script using `updateOne`/`findOneAndUpdate` silently bypasses them with no error. **Applies to every future feature:** Features 06, 07, and 09 must always use fetch-then-`.save()` (with `task.setStatus()` for status changes) for anything these guards protect, never a raw `updateOne`/`findOneAndUpdate` shortcut — the one documented exception is Feature 09's assignment engine, whose entire optimistic-concurrency mechanism is intentionally built on a conditional `findOneAndUpdate` against the `users` collection specifically (not `tasks`/`bids`), which has no guard hook of this kind to bypass in the first place.
- Raw `.collection.insertMany()`/`.insertOne()` (bypassing Mongoose entirely) must never be used anywhere in this codebase for `tasks`/`bids`/`users` documents, including in the seed script — see Feature 03's corrected data-creation pattern.
- **RESOLVED by Feature 08's addendum (previously an accepted known gap):** in `bids.model.js`, the `pre('findOneAndUpdate')`/`pre('save')` guard's shared `runSelfBidAndOpenCheck()` helper was called with a hardcoded `session: null` for its Task lookup. This was accepted as non-blocking on the assumption that bids are created via `save()`/`create()`, not updated later, and don't run inside real transactions. **That assumption no longer holds** — Feature 08's addendum requires bid creation to run inside a real MongoDB transaction (for the audit-write atomicity requirement below), so this gap must now actually be closed: the guard's Task lookup must receive and use the active session.
- **Correction (found during Feature 08 initial review — genuine spec inconsistency, not new scope):** Architecture Blueprint Phase 7 already required `PATCH /tasks/:id/status` and `POST /tasks/:id/bids` to be atomic with their respective audit-log writes (single MongoDB session/transaction). Features 06, 07, and the original version of Feature 08 were specified without this. Corrected via an addendum appended to Feature 08's MD (`docs/features/08-audit-module.md`) rather than a full rewrite. Task creation (`POST /api/tasks`) is explicitly NOT included in this correction — Phase 7 never required it, and a single-document insert is already atomic on its own.
