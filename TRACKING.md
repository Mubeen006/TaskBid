# TaskBid — Build Tracking

> Updated by the Coding Agent after every completed feature. Read this before starting any task.
> Status values: `NOT STARTED` → `IN PROGRESS` → `READY FOR TEST` → `TESTED — READY FOR COMMIT` → `COMMITTED`

| # | Feature | MD File | Status | One-line Summary | Notes / Deviations |
|---|---|---|---|---|---|
| 01 | Project Setup (repo, Docker Compose w/ replica set, env config) | `docs/features/01-project-setup.md` | TESTED — READY FOR COMMIT | Replica set confirmed ok:1 (healthcheck auto-init). Backend Express skeleton + /health route. Deps installed, .env from .env.example. | Replica-set initiation is automatic via Docker Compose healthcheck — no manual step needed. |
| 02 | Database Schema (Mongoose models: users, tasks, bids, auditlogs) | `docs/features/02-database-schema.md` | TESTED — READY FOR COMMIT | All 4 models + guard hooks implemented. Added pre('findOneAndUpdate') guard to both Task and Bid models (Feature 02 updated spec). 4 migrations applied. Indexes verified via mongosh. | Only 1 of 5 constraints (bid unique index) is true DB-engine-level. Guard hooks now cover both pre('save') and pre('findOneAndUpdate'). **Known minor gap, being closed by Feature 08's addendum** — see deviations log below. |
| 03 | Seed Script | `docs/features/03-seed-script.md` | TESTED — READY FOR COMMIT | Seed rewritten with correct pattern: tasks start 'open', Bid.create() while open, status advanced via setStatus()+save(). All 7 statuses covered incl. review. 5 users / 10 tasks / 15 bids. Idempotent (ran twice, same result). | Race-condition test task: 'Migrate legacy auth module' (bidding_closed), lowest bidder Bilal (2h) — a near-capacity user, confirmed suitable for Feature 09's Part-A test. Near-capacity users: Bilal (13/15h), Usman (8/10h). |
| 04 | Validation Schemas (Joi, shared middleware) | `docs/features/04-validation.md` | TESTED — READY FOR COMMIT | validate middleware (abortEarly:false, coerces req[target]), currentUser middleware (missing/malformed/nonexistent → 400), objectIdSchema util, tasks.schema.js, bids.schema.js, users.schema.js. mongoSanitize wired in app.js. 11/11 self-tests pass. | Amended post-signoff: `stripUnknown: true` added to the shared `validate()` factory — discovered during Feature 06 testing. Applies globally (body/params/query, no target-specific branch). Regression-checked against Feature 05's `GET /api/users/:id/workload` endpoint: 4/4 tests pass, no behavior change. |
| 05 | Users Module (workload endpoint) | `docs/features/05-users-module.md` | TESTED — READY FOR COMMIT | capacity.js (getRemainingCapacity, hasCapacityFor — pure, no DB), users.repository.js, users.service.js, users.controller.js. GET /api/users/:id/workload mounted. 8/8 capacity unit tests pass. Live endpoint: valid→200, nonexistent→404, malformed→400. | No deviations. ESLint clean (0 errors). Re-verified 4/4 after Feature 06's stripUnknown fix — no regression. |
| 06 | Tasks Module (create, status transition, list) | `docs/features/06-tasks-module.md` | TESTED — READY FOR COMMIT | tasks.repository/service/controller. POST /api/tasks, PATCH /api/tasks/:id/status, GET /api/tasks (+ optional ?status= filter). Mounted in app.js. Service-level isLegalForwardTransition check + setStatus()+save(). Status-transition now transaction-wrapped with its audit write (Feature 08 addendum). 8/8 original self-tests + retrofit rollback test all pass. | Option (a) for GET — bid-count/lowest-bid closed by Feature 07. No GET /api/tasks/:id. stripUnknown:true (attributed to Feature 04's row). Retrofit confirmed: forced mid-transaction error rolled task.status back to previous value with no orphan audit entry. |
| 07 | Bids Module (place bid, list bids) | `docs/features/07-bids-module.md` | TESTED — READY FOR COMMIT | bids.repository/service/controller. POST+GET /api/tasks/:id/bids mounted in app.js. placeBid uses hasCapacityFor (soft 422), translateBidCreateError maps guard hooks + MongoDB 11000. GET /api/tasks extended with bidCount/lowestBidHours. Bid-creation now transaction-wrapped with its audit write (Feature 08 addendum). 9/9 original self-tests + retrofit rollback test all pass. | Feature 06 follow-up closed. No status/assignment mutation. `session: null` gap in bids.model.js's pre('findOneAndUpdate') guard CLOSED — now reads `this.getOptions().session` in guardSelfBidAndBiddingOpenQuery. Retrofit confirmed: forced Bid-creation abort leaves no orphan bid or audit entry. |
| 08 | Audit Log Module | `docs/features/08-audit-module.md` | TESTED — READY FOR COMMIT | audit.repository.js + audit.service.js built, session-aware. Retrofitted into task creation (non-transactional, correct per Phase 7), task status-transition (transactional), bid creation (transactional). All three produce exactly one audit entry, verified by direct DB query. Illegal transitions/duplicate bids produce zero audit entries. ESLint 0 errors/warnings. | Addendum (transactional retrofit per Architecture Phase 7) fully implemented and tested — see Features 06/07's notes for the specific rollback proofs. Task creation intentionally non-transactional, per Phase 7 scope. |
| 09 | Assignment Engine (transaction + optimistic concurrency) | `docs/features/09-assignment-engine.md` | TESTED — READY FOR COMMIT | assignment.repository/service/controller built. Full algorithm verified via code review: version-conflict (return + retry-whole-attempt) vs capacity-insufficient (continue to next bidder) are structurally distinct, correctly implemented. readConcern:snapshot + writeConcern:majority. Bounded 3-attempt retry loop with TransientTransactionError/UnknownTransactionCommitResult detection. Happy/fallback/no-eligible-bidder/wrong-status paths all pass. **Part-A concurrency test: 10/10 runs pass, retry path confirmed hit in every run (20 log lines = 2 calls × 1 retry each per run), never overflow, capacityVersion incremented exactly once per successful assignment.** | Reuses hasCapacityFor/getRemainingCapacity (Feature 05) and recordChange with session (Feature 08) by name — confirmed in code. `Bid.updateMany()` used for marking other bids not_selected — deliberate, scoped exception (no guard-hook protection applies to this status-only flip on already-validated bids). Full assignment.service.js reviewed line-by-line by the architect. |
| 10 | Dashboard Module (aggregation pipelines) | `docs/features/10-dashboard-module.md` | TESTED — READY FOR COMMIT | GET /api/dashboard/stats. Four separate aggregation pipelines run via Promise.all. All 4 metrics verified against seeded data. ESLint clean. | Parallel four-pipelines design is deliberate per Architecture Blueprint Phase 16 (readability + independent index usage over marginal round-trip savings). Baseline values against fresh seed: tasksByStatus={assigned:1, bidding_closed:1, done:3, draft:1, in_progress:1, open:2, review:1}; avgBidByComplexity={c1:3h, c2:5.5h, c3:4h, c4:6.5h, c5:3.33h}; topUsersByCompleted={Sara Malik:2, Usman Tariq:1}; zeroBidPastDeadline=["Archive old customer records" (deadline 2026-07-23)]. These are the reference values for Feature 16's Dashboard UI. |
| 11 | Swagger/OpenAPI Setup + Backend Error Handling Pass | `docs/features/11-swagger-error-handling.md` | TESTED — READY FOR COMMIT | swagger-ui-express@5.0.1 + swagger-jsdoc@6.2.8 installed. Hand-written OpenAPI 3.0.3 spec at backend/src/docs/swagger.js. All 9 endpoints documented. /api-docs live and returns HTML. 12-point error-handling consistency test passed. Guard-hook discriminator fix applied post-signoff. | Spec approach: hand-written. Error-handling pass: users=clean; tasks=fixed (tasks.model.js pre('save') guard hook now attaches err.isGuardViolation=true before calling next(err); tasks.service.js catch now checks err.isGuardViolation===true instead of message.includes() — string WAS coincidentally matching but discriminator property is the correct, string-independent approach); bids=clean; audit=clean; assignment=clean; dashboard=clean. Both translation paths tested: pre-validation (409 via isLegalForwardTransition check) and guard-hook (409 via isGuardViolation catch) both confirmed. |
| 12 | Frontend Setup (Vite, routing, layout, user-switcher) | `docs/features/12-frontend-setup.md` | NOT STARTED | — | — |
| 13 | Task Board Page | `docs/features/13-task-board.md` | NOT STARTED | — | — |
| 14 | Task Detail Page + Bid Form | `docs/features/14-task-detail.md` | NOT STARTED | — | — |
| 15 | Realtime Layer (Socket.IO, both sides) | `docs/features/15-realtime.md` | NOT STARTED | — | — |
| 16 | Dashboard Page + Chart | `docs/features/16-dashboard-ui.md` | NOT STARTED | — | — |
| 17 | Full Integration Pass + Concurrency Test | `docs/features/17-integration-testing.md` | NOT STARTED | — | — |
| 18 | README + DECISIONS.md | `docs/features/18-readme-decisions.md` | NOT STARTED | — | — |
| 19 | Deployment (Atlas + Render + Vercel) | `docs/features/19-deployment.md` | NOT STARTED | — | — |
| 20 | Bonus Items (if time remains) | `docs/features/20-bonus.md` | NOT STARTED | — | — |

---

## Current Focus
**Feature #12 NOT STARTED — Frontend Setup (Vite, routing, layout, user-switcher).** Features 01–11 are TESTED — READY FOR COMMIT. Backend is complete.

## Blocked Items
None.

## Deviations From Blueprint (running log)
- **Decision (found during Feature 02/03 implementation review):** Feature 02's guard hooks (self-bid, bidding-open, forward-only-status) must be duplicated as `pre('findOneAndUpdate')` query middleware, not just `pre('save')` document middleware — otherwise any service/script using `updateOne`/`findOneAndUpdate` silently bypasses them with no error. **Applies to every future feature:** Features 06, 07, and 09 must always use fetch-then-`.save()` (with `task.setStatus()` for status changes) for anything these guards protect, never a raw `updateOne`/`findOneAndUpdate` shortcut — the one documented exception is Feature 09's assignment engine, whose entire optimistic-concurrency mechanism is intentionally built on a conditional `findOneAndUpdate` against the `users` collection specifically (not `tasks`/`bids`), which has no guard hook of this kind to bypass in the first place.
- Raw `.collection.insertMany()`/`.insertOne()` (bypassing Mongoose entirely) must never be used anywhere in this codebase for `tasks`/`bids`/`users` documents, including in the seed script — see Feature 03's corrected data-creation pattern.
- **RESOLVED (Feature 08 addendum):** in `bids.model.js`, the `pre('findOneAndUpdate')` guard (`guardSelfBidAndBiddingOpenQuery`) previously used a hardcoded `session: null` for its Task lookup. Now reads `this.getOptions().session` and correctly threads the active transaction's session through. Confirmed via a forced-abort test showing no orphan bid or audit entry.
- **RESOLVED (Feature 08 addendum, genuine spec inconsistency, not new scope):** Architecture Blueprint Phase 7 already required `PATCH /tasks/:id/status` and `POST /tasks/:id/bids` to be atomic with their respective audit-log writes (single MongoDB session/transaction). Features 06, 07, and the original version of Feature 08 were specified without this. Corrected via an addendum to Feature 08's MD. Task creation (`POST /api/tasks`) intentionally remains non-transactional — Phase 7 never required it, and a single-document insert is already atomic on its own. Both retrofits verified via forced mid-transaction errors confirming rollback of both the primary document and the audit entry together.